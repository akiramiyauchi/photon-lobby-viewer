[build]
  functions = "functions"
  publish = "public"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["fs", "path", "firebase-admin"]

[[scheduled_functions]]
  name = "cleanup"
  schedule = "cron(0,10,20,30,40,50 * * * *)" # 🔹 10分ごとに実行

